# BountyHunter
Minimal standalone bot scaffold based on the FamilyBot `free_games` plugin (Discord adapter). It uses the `interactions` library and a SQLite-backed store for per-server subscriptions.
